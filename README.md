# bindering

To run:

```
git clone https://github.com/rgbkrk/bindering.git
cd bindering
npm install
npm run start
```

This will:

* Launch a binder backed server
* Launch a kernel
* Query kernel status
* Shutdown the kernel

